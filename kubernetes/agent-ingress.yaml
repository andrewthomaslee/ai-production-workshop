apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agent-ingress
  labels:
    io.kompose.service: agent
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: agent
            port:
              number: 8000
